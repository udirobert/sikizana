# Results index

No Phase 1 benchmark has been run yet. Add only reproducible claims here, each with a run ID and a link to its retained `facts.json` and `provenance.json`.

Do not compare future quantum results to the full-data XGBoost reference unless the feature set, training sample, and test set are equivalent.
