fastapi==0.110.0
uvicorn==0.27.1
python-multipart==0.0.32
pydantic==2.6.3
python-dotenv==1.0.1
httpx==0.27.0
openai==2.44.0
google-generativeai==0.8.6
Pillow==11.3.0
cryptography==44.0.1
stripe==11.4.1
# NOTE: the optional MCP stdio server (src/mcp_server.py) needs the `mcp`
# package, but mcp requires pydantic>=2.7 / starlette>=0.37 / httpx>=0.27.1,
# which conflict with the pinned FastAPI stack above. It is NOT part of the
# production build. The HTTP surface (/api/mcp/ap-scan) does not need it.
# For local MCP development: pip install mcp
